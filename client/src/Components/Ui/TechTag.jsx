
const TechTag = ({ label }) => {
  return (
    <span className="bg-primary/10 border border-primary/20 px-3 py-1 rounded-full text-[10px] font-label-mono text-primary uppercase">
      {label}
    </span>
  );
};

export default TechTag;
